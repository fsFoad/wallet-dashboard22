nodeLinker: node-modules

# Use npm registry through VPN (when VPN is on)
npmRegistryServer: "https://registry.npmjs.org"

# Prefer local cache; avoid network during dev when packages exist
enableOfflineMode: false
